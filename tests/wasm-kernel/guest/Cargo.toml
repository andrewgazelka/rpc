[package]
name = "counter-kernel-guest"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = "0.33"

[profile.release]
opt-level = "s"  # Optimize for size
lto = true
codegen-units = 1
